#include "recursion.h"

int main() {
	while(true) {
		cin >> h;
		switch (h) {
			case '1':
			   cout << "h is a"; break;
			case '2':
			   cout << "h is b"; break;
			case '3': 
		}
	}

	// Pause and exit
	system("PAUSE");
	return 0;
}